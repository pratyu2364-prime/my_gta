// core/constants.js — world-space constants shared across all systems.
// (Stage 0 of the incremental split out of main.js; live-exports model.)
export const WORLD = 450;
export const ROAD_W = 14, SIDE = 3, HALF = ROAD_W / 2 + SIDE;
export const LANE = 3.4;
