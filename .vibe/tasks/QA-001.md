id: QA-001
title: Race only pays out if the player actually raced
phase: 1
depends_on: none
status: todo
attempts: 0
acceptance:
 - startRace() (src/main.js ~line 1961) refuses to start unless the player is in a drivable car; otherwise show a hint ("Get in a car to race") and do not spawn racers.
 - endRace(place) only awards prize money when the player genuinely participated — i.e. crossed at least one race checkpoint (race.lastWp advanced) AND completed on wheels. Standing on foot near the start while the AI finishes must NOT pay out.
 - The existing abandon path (drive away → "Race abandoned", no prize) still works.
 - node scripts/playtest.cjs passes (game starts, no JS/engine errors).
files: [src/main.js]
notes: |
  Bug: startRace() never checks player.inCar, and raceUpdate() abandon check
  (`race.t>6 && !player.inCar && pdist>150`) never trips if the player idles on foot
  near the start ring, so the AI finish 2 laps and endRace(race.pos) hands the player
  a placement prize for doing nothing. Gate the start on a car; gate the payout on real
  participation (track whether the player advanced any checkpoint). Keep it to this one
  exploit — do not redesign the race. Match the compact code style (see CLAUDE.md).
