id: QA-002
title: Enter-vehicle prompt verb matches vehicle type (drive vs ride)
phase: 1
depends_on: none
status: todo
attempts: 0
acceptance:
 - The enter prompt at src/main.js ~line 2251 uses "drive" for cars and "ride" for two-wheelers (bike) and auto-rickshaws, for both the parked ("Press E to drive this car") and stolen ("steal this car") wordings as appropriate.
 - Helicopter/plane (if reachable by this prompt) read "fly" / "pilot", not "ride".
 - No other prompt strings change; onboarding hint (~line 2072) already says "drive" — keep them consistent.
 - node scripts/playtest.cjs passes (game starts, no JS/engine errors).
files: [src/main.js]
notes: |
  Bug: line 2251 builds `'ride this '+what` for any parked vehicle, so a car reads
  "ride this car". "ride" is only correct for bike/auto. Pick the verb from
  near.userData.type (car→drive, bike/auto→ride, heli/plane→fly). Small, format-preserving
  edit — compact style. Good candidate for the cavecrew-builder / a single-file worker pass.
